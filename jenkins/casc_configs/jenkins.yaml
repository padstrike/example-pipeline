jenkins:
  systemMessage: "Configured by JCasC - Do not change manually."
  numExecutors: 2
  mode: NORMAL
  labelString: "built-by-jcasc"
  scmCheckoutRetryCount: 2
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${ADMIN_PASSWORD}"
